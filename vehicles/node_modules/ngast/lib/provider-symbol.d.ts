import { CompileProviderMetadata, CompileMetadataResolver } from '@angular/compiler';
import { Symbol } from './symbol';
import { Program } from 'typescript';
export declare class ProviderSymbol extends Symbol {
    private provider;
    private metadataResovler;
    constructor(program: Program, provider: CompileProviderMetadata, metadataResovler: CompileMetadataResolver);
    getMetadata(): CompileProviderMetadata;
    getDependencies(): ProviderSymbol[];
}
