export declare class CookieLawService {
    seen(cookieName?: string): boolean;
    storeCookie(cookieName: string, expiration?: number): void;
    private cookieExisits(name);
    private setCookie(name, expiration?);
}
